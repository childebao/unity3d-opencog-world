using UnityEngine;
using System.Collections;

/// <summary>
/// OpenCog Action Controller.  Executes OCActions, plays OCAnimations and OCSoundEffects, 
/// and provides feedback on the success or failure of the OCAction.  Only attempts one action at a time,
/// relying on the OCActionScheduler to order/prioritize actions appropriately.
/// </summary>
public class OCActionController : MonoBehaviour
{

	// Use this for initialization
	void Start ()
	{
	
	}
	
	// Update is called once per frame
	void Update ()
	{
	
	}
}

